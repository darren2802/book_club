<h4><%= @author.name %></h4>
<br>
<table class="table">
  <th>Books by this author:</th>
  <% @author.books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
    </tr>
  <% end %>
</table>
<br><br>
<p>Average number of pages across all books: <%= @author.avg_pages %></p>
